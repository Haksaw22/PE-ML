# PoE-ML Program — Track Status Board (as of 2026-07-19)

**Honest headline:** we investigated ONE deep thread (A1 → static-face pivot → sequential spine → active
excitation). Most subpoints were **never formally gated** — they are *un-investigated*, not *shut down*. The
"novel surface is thin" conclusion is a verdict on the investigated thread, **not** on the whole board.

**2026-07-19 round:** a 5-way process audit of prior decisions (`process-audit-2026-07-19.md`) + theory
gates on G2/G3, E1, B3/B4 with a cross-line hunt (`gate-verdicts-2026-07-19.md`). Headlines: the pivot's
core math UPHELD but its *generality* WEAKENED (streaming/demo-arrival axis reopened → A5); the C1–C5
collective kill OVERTURNED at the algorithmic level (citations cover TD-stability backdrop only) → C1–C4
reopened; all three gated lines: **novelty REJECT / instrument REVISE** with concrete repairs; cross-line
hunt found no combined object that outranks the single lines. Build queue reordered by cost-adjusted
information value: **B3/B4-repaired first, E1 second, G-narrow last.**

Legend: 🔬 investigated→demoted/subsumed · ⚗️ investigated→conditional/open · 💤 un-investigated (never gated)
· ⚠️ dropped on a technicality (revisit) · ⏸️ parked · 🎯 now active

---

## A — Few-shot / ICL (frozen-basis)
- **A1** fixed-point / attention-budget PoE — 🔬→🎯. Heavily investigated. Gated REVISE → C1/T4 resolution showed
  the static face does NOT inherit PoE (`λ_min(P^T M_S P)` wrong-object + circular) → corrected to `J(S)` → δ
  pre-empt = **subsumed** by Merhav–Feder + van Trees. **Survives as an INSTRUMENT** (the prior-anisotropy/capacity
  map) = **Spearhead A, now being built.**
- **A2** LPV/LTV-PoE proof route — 💤. Was A1's proof backbone; partially moot once the static face was demoted.
  Never gated standalone.
- **A3** E-optimal / Fisher demo selection — 💤. Flagged machinery-old (Joshi–Boyd, Mukherjee) at ideation; never
  gated standalone; folds into Spearhead A.
- **A4** label-free failure diagnostic + shot law — 🔬. Gated REVISE → **folded into A1/Spearhead A** (not
  independent; the shot≈rank law was tautological). Absorbed.
- **A5** per-task excitation core set (concurrent-learning demo memory) — 💤→🎯 **PROMOTED (audit 2026-07-19):**
  the pivot's axis-orthogonality argument only kills the *single-batch, order-abstracted* static face; the
  streaming/online demo-arrival axis (classical Bitmead–Anderson / concurrent-learning PoE) was never examined
  and remains a live, genuinely distinct PoE reading.
- **A6** excitation-maximizing representations — 🔬 blocked at ideation (Hiller–Harandi–Drummond 2022). Un-blocked
  variant lives on as **G3** (💤).

## B — Algebraic PoE
- **B1** tensor/Kruskal shot law `k*=⌈(2R+m−1)/m⌉` — ⚗️. Gated REVISE. **OPEN** and genuinely live: survives iff the
  effective interaction order `m` is a stable integer >1 — an **empirical question not yet tested**.
- **B2** attention-induced defectivity — ⚗️. Gated REVISE. Empty under softmax *as stated*; the critic un-killed the
  **effective-rank** version. Conditional.
- **B3** nonlinear observability index / unobservable variety — ⚠️→🔬 **GATED 2026-07-19: novelty REJECT /
  instrument REVISE.** Point diagnostic σ_min(J_S) ≈ Fisher-at-a-point = already-subsumed J(S) family, and
  redundant with the Spearhead A instrument; "unobservable variety" template already published
  (Vizuete–Hendrickx 2024). Survives only on variety-*proximity*/curvature content with a redesigned
  confusability test (must beat a generic Lipschitz-smoothness null). B7 relabeled an extension of B3, not
  an independent claim.
- **B4** interval/uniform observability → demo ordering & curriculum — ⚠️→⚗️ **GATED 2026-07-19: novelty
  REJECT (transplant) / instrument REVISE — the healthiest surviving piece.** Static θ through
  prefix-dependent attention maps IS the constant-parameter/time-varying-regressor setting PE was built for
  (prosecution's contradiction charge failed). Windowed score U(π)=min_j λ_min(Ω_j) is mechanistically
  distinct from entropy-based ordering scores (Lu et al. 2022). Repairs before compute: pin window/head
  aggregation, label-free task-relevance account for Ω_j (the "P is not free" tension), existence check for
  the matched-terminal-Gram permutation control.
- **B5** moment-variety degree as shot count — 💤 (adjacent/weak at ideation).
- **B6** Prony / annihilating-filter — 🔬 blocked (arXiv:2501.01312).
- **B7** defective-locus fragility (adversarial few-shot + certification) — 💤. **Never investigated.**
- **B8** cyclic-module / block-Hankel — 🔬→subsumed. Sound but **classical** (Ho-Kalman/N4SID/Willems); no new
  functional. Possible instrument reframe.

## C — RL / online excitation (moving-basis)
- **C1–C5** — 🔬→💤 **COLLECTIVE KILL OVERTURNED (audit 2026-07-19).** Kolter 2011 / Bhandari 2018 /
  Ghosh–Bellemare 2020 establish only the *necessary-condition backdrop* (TD stability/rate governed by
  feature-covariance conditioning); none propose or analyze active per-skill excitation scheduling (C1/C2),
  self-terminating per-mode exploration (C3), or excitation-aware replay curation (C4). C1–C4 revert to
  **un-gated/open**, pending a real prior-art pass on curriculum RL, prioritized replay with info-gain
  criteria, and skill-conditioned exploration bonuses (never checked). The per-skill quarantine wedge stays
  live. Cross-line note: B4's windowed Gram and C3/C4 per-mode scheduling are literally the same object —
  filed as optional follow-on under G's build order, contingent on both parents.

## D — Info-geometry / experiment-design engines
- **D1–D3** — ⏸️ parked. Un-investigated.

## E — Supercharge applications
- **E1** SAE feature identifiability + concurrent-learning repair — 💤→⚗️ **GATED 2026-07-19: novelty REJECT /
  instrument REVISE.** The "never touched" framing was STALE: a dedicated 2025–26 SAE-identifiability
  literature now exists (per-feature activation-richness theorem arXiv:2506.14002; identifiability+repair
  arXiv:2512.05534) and must be reconciled first-hand. The judge rejected the prosecution's circularity-kill
  (α_j at a fitted parameter = standard local-Fisher/M-estimation practice, NOT the A1 tautology), but the
  written non-circularity argument is owed. Residual wedge (narrow but real, unclaimed): an **online-monitorable**
  excitation condition during streaming SAE training + a **buffer-based concurrent-learning repair with a
  convergence proof** (vs. the published architectural-anchoring repair). Repairs: matched-null control for the
  frequency/volume confound in K1; geometry-matched null for feature-splitting; honest costing (needs a
  multi-seed SAE training campaign — Gemma Scope is single-seed).
- **E2** world-model excitation × observability × value — 💤 (deep bet, un-gated).
- **E3** RLHF on-policy PE gate — 💤. You promoted it to "explore" but **we never reached it.**
- **E4** pretraining E-optimal data-mixture SDP — 💤 (engineering).
- **E5** reframes — continual-forgetting / SSL / LoRA = blocked; unlearning-cert / uncertainty-calibration = un-gated.

## F — Differential / continuous PoE
- **F1** differential PoE (gradient-flow excitation inequality) — ⏸️ parked. Un-investigated. **Audit note
  (2026-07-19): the ONE parked line with zero negative signal anywhere in the documents** — worth a fresh
  ideation pass ahead of D (which likely collides with the same Kiefer–Wolfowitz/Joshi–Boyd wall that
  dissolved A1's original claim).

## G — Independent-mode / block-diagonal excitation
- **G1** separability ⟺ block-diagonal excitation — 🔬→conditional. Gated REVISE (wrong operator M_S; reframe to
  TD/Hankel). Conditional.
- **G2** output-correspondence / CCA modes — 💤→🔬 **GATED 2026-07-19: novelty REJECT (high conf) — the decisive
  kill of the round.** M*_static = Σφφ^{-1/2}ΣφyΣyy^{-1}ΣyφΣφφ^{-1/2} has rank ≤ dim(y) (standard CCA algebra),
  so for scalar/low-dim outputs — the football corollary's own reward and the note's own Garg-style benchmark —
  at most ONE block can be PE: the k≥2 static certificate is **unsatisfiable by construction**. Conceded by the
  defense. The static/sequential "same shifted-moment operator" unification claim is also false as stated
  (M*_seq is a 4-term lag combination, not a Hankel slice). G2 survives only if rescoped to dim(y) ≥ k
  multi-task outputs, which presupposes the supervision it was meant to discover.
- **G3** block-diagonalizing representation learning — 💤→🔬 **GATED 2026-07-19: instrument REJECT-as-filed.**
  The learned-φ objective optimizes its own success criterion — a flexible modular φ will always produce a
  low-ρ high-α split whether or not it tracks real skill structure (sign-over-determination). Survivor of the
  whole G line: a **narrow sequential/RL-only TD-residual-Gram certificate** (M*_seq = E[(φ_t−γφ_{t+1})(·)^T],
  full-rank, no output ceiling) under an **externally pre-given architectural partition**, argued against
  Bhandari–Russo–Singal 2018 / Srikant–Ying 2019 on the per-block-rate layer; primary falsifiable deliverable =
  the two-sided divergence tests vs eigenoptions/DIAYN. The O(κ²ρ²) law is generic Neumann perturbation — not
  discriminating evidence.
- **G4** dynamic skill segmentation (football) — ⏸️ parked. **G5** global=local objective — ⏸️ parked.
- **G6** RL mode-decomposition program — 🔬→subsumed (via sequential gate).

---

## The honest flags — resolution status (2026-07-19)
1. **E1** — now gated (novelty REJECT / instrument REVISE; see E row). Stale-literature problem found.
2. **B3 / B4** — now gated (B4 is the healthiest surviving instrument candidate; see B rows).
3. **G2** — now gated and killed on the merits (rank-collapse; see G row). Properly examined at last.
4. **A5** — PROMOTED (streaming axis reopened by pivot audit). **B7** — relabeled B3-extension. **E2/E3** —
   explicitly low-priority: E2 inherits B3's negative priors, E3 is a C-family instance (now reopened with C).

## Spearhead outcomes (both concluded)
- **Spearhead A** — 🔬 concluded: **"working instrument" claim WEAKENED (audit 2026-07-19, medium conf).**
  Real, directionally-consistent signal (proj beats full and beats k in every slice) BUT: tested object is the
  PRE-pivot naive projected Gram (attention-weighted object never computed anywhere); P is oracle-injected
  (first R raw coordinates, no rotation); the 4.88× quartile ratio is provably k-confounded (Weyl; never
  recomputed at fixed k — treat as upper bound); proj-vs-full gaps (0.04–0.08, n≈170) not statistically
  established; no ridge/OLS floor control on the same contexts; missing G-optimal tr(Pᵀ(G+Λ)^{-1}P) baseline
  (self-flagged in the note §7.4); A1/A1b outputs unsaved; eval-model convergence unverified; zero signature
  predictions tested. 7-item repair list in `process-audit-2026-07-19.md` Audit 4 — required before the
  instrument is cited again.
- **Spearhead B** — 🔬 concluded: REFUTED/SUBSUMED per `experiments/spearhead_b/RESULTS.md` — **but audit
  2026-07-19 WEAKENS the verdict's generality:** it holds only in the linear-Gaussian, free-design,
  oracle-informed, one-shot-batch box. Math/MC validated; however RESULTS.md overrode its own pre-registered
  magnitude-separation signal (script printed "SEPARATE", 0.5632 > 0.5) without reconciling, and the N=30
  over-hedge datapoint was never MC-validated. **Resurrection path exists:** the sequential/adaptive version
  with θ_c estimated online — the version actually faithful to PE-as-adaptive-control — was never built
  (~25–35% chance of a distinct phenomenon; constrained-design variant ~25–30%, hours of work). See
  `process-audit-2026-07-19.md` Audit 3.

## Build queue (2026-07-19, audit-reordered by cost-adjusted information value)
1. 🎯 **B3/B4-repaired** — near-zero-cost resurrection, already-designed gate; B4-centric (windowed
   uniform-observability ordering) with the listed repairs closed BEFORE compute.
2. 🎯 **E1-narrowed** — ideation/write-up pass first (not yet gate-ready as ranked); reconcile the 2025–26
   SAE-identifiability literature first-hand; target the online-monitorable-condition + provable-buffer-repair
   wedge; instrument bar only.
3. **G-narrow** — re-file ONLY the sequential TD-residual-Gram certificate under an external partition;
   two-sided eigenoptions/DIAYN divergence tests as the deliverable. Static face and learned-φ discovery
   objective are dead as filed.
4. (follow-on, contingent) **B4-as-per-mode-scheduler** = the C3/C4 revival under G's partition — the one
   well-formed cross-line object; requires the unchecked prioritized-replay/info-gain-curriculum prior-art
   pass first.

5. (optional, cheap) **Spearhead B resurrection probes** — constrained-design variant (hours, ~25–30%) and/or
   the sequential/adaptive estimated-θ_c version (~25–35%; the PE-faithful construction that was never built).
   Even nulls upgrade the "(b) subsumed" verdict from one-corner to general.
6. (cheap re-analysis) **Spearhead A repair package** — 7 items in `process-audit-2026-07-19.md` Audit 4
   (fixed-k quartiles, paired stats on saved arrays, ridge floor, G-optimal baseline, one signature test).

**Also reopened:** the 75–80% sequential-spine reweight (three independent post-pivot data points all point
toward LESS confidence in that spine); the pivot's generality (now scoped to the single-batch static face).
